services:

  mongodb:
    image: mongo:latest
    container_name: medilabo-mongodb

    ports:
      - "27017:27017"

    volumes:
      - mongodb-data:/data/db
      - ./note-service/docker/init-mongo.js:/docker-entrypoint-initdb.d/init-mongo.js:ro

volumes:
  mongodb-data: